---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: emqx-dashboard
spec:
  ingressClassName: internal
  rules:
    - host: "emqx.${CLOUDFLARE_DOMAIN}"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: emqx-dashboard
                port:
                  number: 18083
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: emqx-websocket
spec:
  ingressClassName: internal
  rules:
    - host: "mqtt.${CLOUDFLARE_DOMAIN}"
      http:
        paths:
          - path: /mqtt
            pathType: Prefix
            backend:
              service:
                name: emqx-websocket
                port:
                  number: 8083
